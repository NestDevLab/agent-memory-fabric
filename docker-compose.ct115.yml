services:
  mem0-gateway:
    image: node:22-bookworm-slim
    working_dir: /app
    command: ["node", "src/server.mjs"]
    restart: unless-stopped
    env_file:
      - .env.runtime
    volumes:
      - ./:/app
    ports:
      - "8787:8787"
