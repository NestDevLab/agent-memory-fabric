#!/usr/bin/env node
import path from 'node:path';
import { fileURLToPath } from 'node:url';
import { planM4CrossPhaseIdentityTraversalOperator, runM4CrossPhaseIdentityTraversalOperator } from '../src/operator/m4-cross-phase-identity-traversal-operator.mjs';
function fail(code){const error=new Error(code);error.code=code;throw error;}
function parse(argv){try{const operation=argv[2];if(!['plan','run'].includes(operation))fail('m4_cross_phase_identity_traversal_operator_argument_invalid');const required=operation==='plan'?['--config']:['--config','--confirmed-plan-digest'];const values={};for(let index=3;index<argv.length;index+=2){const key=argv[index],value=argv[index+1];if(!required.includes(key)||Object.hasOwn(values,key)||typeof value!=='string'||!value||value.startsWith('--'))fail('m4_cross_phase_identity_traversal_operator_argument_invalid');values[key]=value;}if(Object.keys(values).length!==required.length||required.some(key=>values[key]===undefined)||!path.isAbsolute(values['--config'])||path.normalize(values['--config'])!==values['--config']||(operation==='run'&&!/^sha256:[a-f0-9]{64}$/.test(values['--confirmed-plan-digest'])))fail('m4_cross_phase_identity_traversal_operator_argument_invalid');return {operation,input:{configPath:values['--config'],...(operation==='run'?{confirmedPlanDigest:values['--confirmed-plan-digest']}: {})}};}catch(error){if(error?.code?.startsWith?.('m4_cross_phase_identity_'))throw error;fail('m4_cross_phase_identity_traversal_operator_argument_invalid');}}
export async function runM4CrossPhaseIdentityTraversalCli(argv=process.argv){const request=parse(argv);return request.operation==='plan'?planM4CrossPhaseIdentityTraversalOperator(request.input):runM4CrossPhaseIdentityTraversalOperator(request.input);}
const main=process.argv[1]&&fileURLToPath(import.meta.url)===path.resolve(process.argv[1]);
if(main)runM4CrossPhaseIdentityTraversalCli().then(value=>process.stdout.write(`${JSON.stringify({ok:true,...value})}\n`)).catch(error=>{const code=typeof error?.code==='string'&&error.code.startsWith('m4_cross_phase_identity_')?error.code:'m4_cross_phase_identity_traversal_operator_failed';process.stderr.write(`${JSON.stringify({ok:false,error:code})}\n`);process.exitCode=78;});
