import crypto from 'node:crypto';
import fs from 'node:fs';
import path from 'node:path';

import { canonicalJson } from '../ingest/transcripts/canonical.mjs';
import { createM4CrossPhaseIdentityTraversalRecord, verifyM4CrossPhaseIdentityTraversalRecord } from './m4-cross-phase-identity-traversal-completion.mjs';

const SCHEMA = 'amf.m4-cross-phase-identity-traversal-store/v1';
const ID = /^[a-z][a-z0-9-]{2,79}$/;
const LMSG = /^lmsg_[a-f0-9]{64}$/;
const DIGEST = /^sha256:[a-f0-9]{64}$/;
const MAX_BYTES = 1_048_576;

function fail(code) { const error = new Error(code); error.code = code; throw error; }
function plain(value) { return value !== null && typeof value === 'object' && !Array.isArray(value) && Object.getPrototypeOf(value) === Object.prototype; }
function exact(value, keys) { return plain(value) && Object.keys(value).length === keys.length && keys.every(key => Object.hasOwn(value,key)); }
function clone(value, code) { try { return structuredClone(value); } catch { fail(code); } }
function same(left,right) { return canonicalJson(left) === canonicalJson(right); }
function digest(value) { return `sha256:${crypto.createHash('sha256').update(canonicalJson(value),'utf8').digest('hex')}`; }
function owner() { return typeof process.geteuid === 'function' ? process.geteuid() : typeof process.getuid === 'function' ? process.getuid() : null; }
function assertDirectory(stat) { if (!stat.isDirectory() || stat.isSymbolicLink() || (stat.mode & 0o777) !== 0o700 || (owner() !== null && stat.uid !== owner())) fail('m4_cross_phase_identity_traversal_store_unsafe'); }
function assertFile(stat) { if (!stat.isFile() || stat.isSymbolicLink() || (stat.mode & 0o777) !== 0o600 || (owner() !== null && stat.uid !== owner())) fail('m4_cross_phase_identity_traversal_store_unsafe'); }
function root(value) { if (typeof value !== 'string' || !path.isAbsolute(value) || path.resolve(value) !== value) fail('m4_cross_phase_identity_traversal_store_dependency_invalid'); return value; }
function binding(value, code) { if (!exact(value,['runId','planDigest','catalogBaselineDigest']) || !ID.test(value.runId) || ![value.planDigest,value.catalogBaselineDigest].every(item => typeof item === 'string' && DIGEST.test(item))) fail(code); return clone(value,code); }
function checkpoint(value, code) { if (!exact(value,['id','digest']) || !ID.test(value.id) || !DIGEST.test(value.digest)) fail(code); return clone(value,code); }
export function createM4CrossPhaseIdentityTraversalGroupCheckpoint(input = {}) { const safe=clone(input,'m4_cross_phase_identity_traversal_store_group_invalid'); if (!exact(safe,['sequence','logicalMessageId','outcome','identityBlockDigest']) || !Number.isSafeInteger(safe.sequence) || safe.sequence < 1 || safe.sequence > 2_000_000 || !LMSG.test(safe.logicalMessageId) || !['accepted','excluded'].includes(safe.outcome) || !((safe.outcome === 'accepted' && typeof safe.identityBlockDigest === 'string' && DIGEST.test(safe.identityBlockDigest)) || (safe.outcome === 'excluded' && safe.identityBlockDigest === null))) fail('m4_cross_phase_identity_traversal_store_group_invalid'); const payload=['amf.m4-cross-phase-identity-traversal-store/v1/group-checkpoint',safe.sequence,safe.logicalMessageId,safe.outcome,safe.identityBlockDigest]; return Object.freeze({id:`m4id-${crypto.createHash('sha256').update(canonicalJson(payload),'utf8').digest('hex')}`,digest:digest(payload)}); }
function group(value, code) { const safe=clone(value,code); if (!exact(safe,['sequence','checkpoint','logicalMessageId','outcome','identityBlockDigest'])) fail(code); const expected=createM4CrossPhaseIdentityTraversalGroupCheckpoint({sequence:safe.sequence,logicalMessageId:safe.logicalMessageId,outcome:safe.outcome,identityBlockDigest:safe.identityBlockDigest}); if (!same(checkpoint(safe.checkpoint,code),expected)) fail(code); return { sequence:safe.sequence, checkpoint:expected, logicalMessageId:safe.logicalMessageId, outcome:safe.outcome, identityBlockDigest:safe.identityBlockDigest }; }
function chain(result, prior) { return digest(['amf.m4-cross-phase-identity-traversal-store/v1/chain',prior,result]); }
function traversalDigest(value) { return digest(['amf.m4-cross-phase-identity-traversal-store/v1/traversal',value.traversalChain,value.acceptedGroupCount,value.excludedGroupCount]); }
function initial(bindingValue) { const traversalChain=digest(['amf.m4-cross-phase-identity-traversal-store/v1/chain','initial',bindingValue]); return { schema:SCHEMA,binding:clone(bindingValue,'m4_cross_phase_identity_traversal_store_state_invalid'),sequence:0,checkpoint:null,acceptedGroupCount:0,excludedGroupCount:0,traversalChain,traversalDigest:digest(['amf.m4-cross-phase-identity-traversal-store/v1/traversal',traversalChain,0,0]),lastResultDigest:null,complete:false,record:null }; }
function state(value, expected, code) {
  const safe=clone(value,code); if (!exact(safe,['schema','binding','sequence','checkpoint','acceptedGroupCount','excludedGroupCount','traversalChain','traversalDigest','lastResultDigest','complete','record']) || safe.schema !== SCHEMA || !same(binding(safe.binding,code),expected) || !Number.isSafeInteger(safe.sequence) || safe.sequence < 0 || safe.sequence > 2_000_000 || !Number.isSafeInteger(safe.acceptedGroupCount) || safe.acceptedGroupCount < 0 || !Number.isSafeInteger(safe.excludedGroupCount) || safe.excludedGroupCount < 0 || safe.acceptedGroupCount + safe.excludedGroupCount !== safe.sequence || !DIGEST.test(safe.traversalChain) || !DIGEST.test(safe.traversalDigest) || safe.traversalDigest !== traversalDigest(safe) || !(safe.sequence === 0 ? safe.checkpoint === null && safe.lastResultDigest === null : safe.checkpoint !== null && typeof safe.lastResultDigest === 'string' && DIGEST.test(safe.lastResultDigest)) || typeof safe.complete !== 'boolean' || !((safe.complete && safe.record !== null) || (!safe.complete && safe.record === null))) fail(code);
  if (safe.checkpoint !== null) { checkpoint(safe.checkpoint,code); if (safe.checkpoint.digest !== safe.lastResultDigest || safe.checkpoint.id !== `m4id-${safe.lastResultDigest.slice(7)}`) fail(code); }
  if (safe.complete) { const record=verifyM4CrossPhaseIdentityTraversalRecord(safe.record); if (record.runId !== expected.runId || record.planDigest !== expected.planDigest || record.catalogAttestationDigest !== expected.catalogBaselineDigest || record.traversalDigest !== safe.traversalDigest || record.acceptedGroupCount !== safe.acceptedGroupCount || record.excludedGroupCount !== safe.excludedGroupCount) fail(code); }
  return safe;
}
function existingComponents(rootPath) { const parsed=path.parse(rootPath); let current=parsed.root; for (const segment of rootPath.slice(parsed.root.length).split(path.sep).filter(Boolean)) { current=path.join(current,segment); try { const stat=fs.lstatSync(current); if (stat.isSymbolicLink() || !stat.isDirectory()) fail('m4_cross_phase_identity_traversal_store_unsafe'); } catch (error) { if (error?.code === 'ENOENT') return; if (error?.code?.startsWith?.('m4_cross_phase_identity_traversal_store_')) throw error; fail('m4_cross_phase_identity_traversal_store_unsafe'); } } }
function openDirectory(rootPath) { existingComponents(rootPath); let descriptor; try { fs.mkdirSync(rootPath,{recursive:true,mode:0o700}); descriptor=fs.openSync(rootPath,fs.constants.O_RDONLY | (fs.constants.O_DIRECTORY ?? 0) | (fs.constants.O_NOFOLLOW ?? 0)); assertDirectory(fs.fstatSync(descriptor)); return descriptor; } catch (error) { if (descriptor !== undefined) try { fs.closeSync(descriptor); } catch {} if (error?.code?.startsWith?.('m4_cross_phase_identity_traversal_store_')) throw error; fail('m4_cross_phase_identity_traversal_store_unsafe'); } }
function child(fd,name) { return `/proc/self/fd/${fd}/${name}`; }
function lstat(fd,name) { try { return fs.lstatSync(child(fd,name)); } catch (error) { if (error?.code === 'ENOENT') return null; fail('m4_cross_phase_identity_traversal_store_unsafe'); } }
function sync(fd) { try { fs.fsyncSync(fd); } catch { fail('m4_cross_phase_identity_traversal_store_durability_failed'); } }
function recover(fd,name) { let names; try { names=fs.readdirSync(child(fd,'.')); } catch { fail('m4_cross_phase_identity_traversal_store_unsafe'); } const expression=new RegExp(`^\\.${name}\\.[a-f0-9-]{36}\\.tmp$`); for (const item of names) { if (!expression.test(item)) continue; const stat=lstat(fd,item); if (stat !== null) { assertFile(stat); try { fs.unlinkSync(child(fd,item)); } catch { fail('m4_cross_phase_identity_traversal_store_unsafe'); } } } sync(fd); }
function read(fd,name,expected) { const before=lstat(fd,name); if (before === null) return initial(expected); assertFile(before); let handle; try { handle=fs.openSync(child(fd,name),fs.constants.O_RDONLY | (fs.constants.O_NOFOLLOW ?? 0)); const opened=fs.fstatSync(handle); assertFile(opened); if (opened.dev !== before.dev || opened.ino !== before.ino || opened.size > MAX_BYTES) fail('m4_cross_phase_identity_traversal_store_unsafe'); const bytes=fs.readFileSync(handle); const after=fs.fstatSync(handle); if (after.dev !== opened.dev || after.ino !== opened.ino || after.size !== opened.size || after.mtimeMs !== opened.mtimeMs || after.ctimeMs !== opened.ctimeMs || bytes.length > MAX_BYTES) fail('m4_cross_phase_identity_traversal_store_unsafe'); let parsed; try { parsed=JSON.parse(bytes.toString('utf8')); } catch { fail('m4_cross_phase_identity_traversal_store_corrupt'); } return state(parsed,expected,'m4_cross_phase_identity_traversal_store_corrupt'); } catch (error) { if (error?.code?.startsWith?.('m4_cross_phase_identity_traversal_store_')) throw error; fail('m4_cross_phase_identity_traversal_store_unsafe'); } finally { if (handle !== undefined) fs.closeSync(handle); } }
function write(fd,name,value) { const bytes=Buffer.from(canonicalJson(value),'utf8'); if (bytes.length > MAX_BYTES) fail('m4_cross_phase_identity_traversal_store_state_invalid'); const temporary=`.${name}.${crypto.randomUUID()}.tmp`; let handle; try { const prior=lstat(fd,name); if (prior !== null) assertFile(prior); handle=fs.openSync(child(fd,temporary),fs.constants.O_WRONLY | fs.constants.O_CREAT | fs.constants.O_EXCL | (fs.constants.O_NOFOLLOW ?? 0),0o600); fs.fchmodSync(handle,0o600); fs.writeFileSync(handle,bytes); fs.fsyncSync(handle); fs.closeSync(handle); handle=undefined; fs.renameSync(child(fd,temporary),child(fd,name)); sync(fd); } catch (error) { if (error?.code?.startsWith?.('m4_cross_phase_identity_traversal_store_')) throw error; fail('m4_cross_phase_identity_traversal_store_durability_failed'); } finally { if (handle !== undefined) fs.closeSync(handle); try { fs.unlinkSync(child(fd,temporary)); } catch (error) { if (error?.code !== 'ENOENT') fail('m4_cross_phase_identity_traversal_store_durability_failed'); } } }

export class M4CrossPhaseIdentityTraversalStore {
  constructor({ rootPath, runId, planDigest, catalogBaselineDigest } = {}) { this.binding=binding({runId,planDigest,catalogBaselineDigest},'m4_cross_phase_identity_traversal_store_dependency_invalid'); this.rootPath=root(rootPath); this.name=`m4-cross-phase-identity-traversal-${digest(this.binding).slice(7)}.json`; this.directory=openDirectory(this.rootPath); try { recover(this.directory,this.name); } catch (error) { fs.closeSync(this.directory); this.directory=null; throw error; } }
  load() { if (this.directory === null) fail('m4_cross_phase_identity_traversal_store_closed'); return clone(read(this.directory,this.name,this.binding),'m4_cross_phase_identity_traversal_store_corrupt'); }
  commit(input) { if (this.directory === null) fail('m4_cross_phase_identity_traversal_store_closed'); const next=group(input,'m4_cross_phase_identity_traversal_store_group_invalid'); const current=this.load(); if (current.complete) fail('m4_cross_phase_identity_traversal_store_complete'); const resultDigest=next.checkpoint.digest; if (next.sequence === current.sequence) { if (current.sequence < 1 || current.lastResultDigest !== resultDigest) fail('m4_cross_phase_identity_traversal_store_drift'); return clone(next,'m4_cross_phase_identity_traversal_store_group_invalid'); } if (next.sequence !== current.sequence + 1) fail('m4_cross_phase_identity_traversal_store_gap'); const value={ ...current, sequence:next.sequence, checkpoint:next.checkpoint, acceptedGroupCount:current.acceptedGroupCount+(next.outcome === 'accepted' ? 1 : 0), excludedGroupCount:current.excludedGroupCount+(next.outcome === 'excluded' ? 1 : 0), traversalChain:chain(resultDigest,current.traversalChain), lastResultDigest:resultDigest }; value.traversalDigest=traversalDigest(value); write(this.directory,this.name,value); return clone(next,'m4_cross_phase_identity_traversal_store_group_invalid'); }
  complete({ expectedGroupCount } = {}) { if (this.directory === null) fail('m4_cross_phase_identity_traversal_store_closed'); if (!Number.isSafeInteger(expectedGroupCount) || expectedGroupCount < 1 || expectedGroupCount > 2_000_000) fail('m4_cross_phase_identity_traversal_store_completion_invalid'); const current=this.load(); if (current.complete) { if (current.sequence !== expectedGroupCount) fail('m4_cross_phase_identity_traversal_store_completion_invalid'); return clone(current.record,'m4_cross_phase_identity_traversal_store_corrupt'); } if (current.sequence !== expectedGroupCount || current.acceptedGroupCount + current.excludedGroupCount !== expectedGroupCount) fail('m4_cross_phase_identity_traversal_store_completion_invalid'); const record=createM4CrossPhaseIdentityTraversalRecord({runId:this.binding.runId,planDigest:this.binding.planDigest,traversalDigest:current.traversalDigest,catalogAttestationDigest:this.binding.catalogBaselineDigest,acceptedGroupCount:current.acceptedGroupCount,excludedGroupCount:current.excludedGroupCount}); const value={...current,complete:true,record}; write(this.directory,this.name,value); return clone(record,'m4_cross_phase_identity_traversal_store_completion_invalid'); }
  close() { if (this.directory !== null) { fs.closeSync(this.directory); this.directory=null; } }
}
